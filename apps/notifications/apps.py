"""apps.notifications — App Config"""
from django.apps import AppConfig

class NotificationsConfig(AppConfig):
    name = "apps.notifications"
    label = "notifications"
    verbose_name = "Live Notifications"
